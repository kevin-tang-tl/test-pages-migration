server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html; # Points to the mounted 'sites' directory
        index index.html index.htm;
        try_files $uri $uri/ =404;
    }
}